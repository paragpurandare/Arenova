spring.application.name=arenova-sports-ecosystem

#DB config
spring.datasource.url=jdbc:mysql://localhost:3306/arenovaDB?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=parag
spring.datasource.password=paragpurandare2003
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
#default server port 8080
#server.port=9090

# JWT config
# NOTE: for production, move jwt.secret to an environment variable, e.g. ${JWT_SECRET}
# Must be at least 32 characters (256 bits) for the HS256 signing algorithm.
jwt.secret=arenova-sports-ecosystem-super-secret-signing-key-change-me-in-prod
jwt.expiration=86400000

# CORS - allowedOriginPatterns supports wildcard * even with credentials enabled.
app.cors.allowed-origins=*